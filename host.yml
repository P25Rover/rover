nodes:
  - id: electrode
    custom:
      build: cargo build -p electrode
      source: target/debug/electrode
      args: --listener localhost:4242 --sender localhost:4243
      inputs:
        tick: dora/timer/millis/17
      outputs:
        - camera_image
        - vision_image
        - vision_command
        - lidar_scan