nodes:
  - id: electrode
    custom:
      build: cargo build -p electrode
      source: target/debug/electrode
      args: --address localhost --listener 4242 --sender 4243
      inputs:
        tick: dora/timer/millis/17
      outputs:
        - camera_image
        - vision_image
        - vision_command
        - lidar_scan