nodes:
  - id: electrode
    custom:
      build: cargo build -p electrode
      source: target/debug/electrode
      args: --address localhost:7447 --listener host --sender navqplus
      inputs:
        tick: dora/timer/millis/17
      outputs:
        - camera_image
        - vision_image
        - vision_command
        - lidar_scan